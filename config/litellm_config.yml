# Initial LiteLLM configuration
# After first run, manage via web UI at http://127.0.0.1:4000

# General settings
general_settings:
  master_key: os.environ/LITELLM_MASTER_KEY
  database_url: os.environ/DATABASE_URL
  ui_access_mode: "admin"  # Add this line
